import 'package:flutter/material.dart';

class AppColors {
  // Auth
  static const Color authPrimary = Color(0xFF2979FF);
  static const Color authSecondary = Color(0xFF7B61FF);

  // App general
  static const Color background = Color(0xFFF5F7FB);
  static const Color surface = Colors.white;
  static const Color card = Colors.white;
  static const Color border = Color(0xFFE4E7EC);

  // Textos
  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textWhite = Colors.white;

  // Acciones
  static const Color primary = Color(0xFF2979FF);
  static const Color primaryDark = Color(0xFF1565C0);
  static const Color like = Color(0xFFE53935);
  static const Color success = Color(0xFF2E7D32);
  static const Color danger = Color(0xFFD32F2F);

  // Inputs / overlays
  static const Color inputFill = Color(0xFFF9FAFB);
  static const Color overlayDark = Color(0x66000000);
  static const Color shadow = Color(0x1A000000);

  static get secondary => null;
}